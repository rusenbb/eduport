<!--
	See ../+layout.svelte — the workspace lives there so that
	navigation between /[type] and /[type]/[fileId] does NOT remount
	the component, which used to trigger a full list refetch on every
	row click.
-->
